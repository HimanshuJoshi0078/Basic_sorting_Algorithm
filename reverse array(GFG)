#include<iostream>
using namespace std;
int main(){
    class Solution{
        void reverseArray(int arr[], int n){
            int i = 0;
            int j = n-1;
            while(i < j){
                swap(arr[i],arr[j]);
                i++;
                j--;
            }
        }
        
    }
}
